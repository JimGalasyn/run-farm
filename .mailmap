# Canonical identities for git log / shortlog / blame.
#
# Unlike the sibling repos (jax-solitons, jax-morpho, Morphospace), no work address
# has ever landed here: all 14 commits as of 2026-07-31 are authored AND committed as
# jim.galasyn@hotmail.com.
#
# WHY it has not recurred is per-machine, and this comment used to give only the work
# machine's answer as though it were the answer:
#
#   - work machine: a conditional include keyed on the REMOTE URL, so the personal
#     identity attaches to any clone of this repo anywhere on disk —
#       [includeIf "hasconfig:remote.*.url:https://github.com/JimGalasyn/**"]
#               path = ~/.gitconfig-personal
#   - Bender (verified 2026-07-31): no conditional include and no ~/.gitconfig-personal.
#     The global user.email is simply the personal address, and the work address is not
#     configured at all. This checkout ALSO carries a repo-local user.email — which
#     jax-morpho and jax-solitons do not, so do not generalise from it.
#
# Neither is enforceable by the repo, so a new machine is unguarded until it is set up.
# Check `git config user.email` there before the first commit.
#
# So only one of the two lines below fixes anything today:
#
#   * The NAME split is real: "Jim" (10 commits) and "Jim Galasyn" (7) are the same
#     person, and `git shortlog -sne` reported two authors before this file existed.
#     The second line collapses them.
#   * The confluent.io line is PRECAUTIONARY — no commit here has ever carried that
#     address — but do not read that as idle. It rests on a prediction that turns out to
#     be TRUE: the GitHub account's public email is jim.galasyn@confluent.io, and a
#     squash-merged PR can be re-attributed to it. Verified 2026-08-01 by comparing
#     PRE-SQUASH branch authors with the squashed result, which is the comparison the
#     earlier round never made: exoclimate #1 (`58ce3be` hotmail -> `6aab14d` confluent)
#     and proc-warden #11 (`50fb8fa` hotmail -> `235f509` confluent), plus
#     soliton-playground #1 (`337084c` confluent). It is INTERMITTENT — jax-solitons #74
#     kept the branch author five seconds before proc-warden #11 did not — so a sample
#     of three negatives said nothing. The trigger is undetermined, and two explanations
#     are now ruled out (checked 2026-08-01 with the `user:email` scope): the account did
#     not change — `gh api user/emails` reports jim.galasyn@confluent.io still PRIMARY,
#     verified and public, the state it was in when this fired — and the merge path
#     leaves no trace, all seven 07-27-era squash commits carrying committer
#     `GitHub <noreply@github.com>` with a valid signature.
#
#     NOT SQUASH-SPECIFIC, measured 2026-08-01. GitHub re-attributes any commit it
#     composes on the account's behalf, merge commits included: jax-solitons PR #77,
#     merged with `--merge`, has two parents both authored jim.galasyn@hotmail.com and
#     a merge commit `b33f0a2` authored jim.galasyn@confluent.io. That firing is also
#     more recent than every other on record — earlier ones were all 2026-07-27 or
#     before — so the behaviour is live, not historical.
#
#     A LOCAL merge (`git merge --no-ff` then push) is authored from your own config
#     and avoids it, unless a ruleset demands code-scanning results for the pushed
#     commit; a locally-made merge commit can never satisfy that, since GitHub cannot
#     scan a commit it has never seen.
#
#     This repo lands direct commits on main, so it has not been exposed yet; the line
#     is what keeps the FIRST merged PR here — of either kind — from introducing a
#     second author.
#
#     The three negatives that the previous version of this comment reported —
#     Morphospace `60b138b` (#31), jax-solitons `918eda7` (#76), jax-morpho `0dc9a3a`
#     (#9), all landing as hotmail — are real observations and are left standing. What
#     was wrong was the conclusion drawn from them — and it is worth being exact about
#     what the error was NOT. Those three checks COULD have failed: the account email is
#     confluent.io while the observed outputs were hotmail, so "GitHub preserved the
#     branch author" and "GitHub re-attributed to the account email" predict DIFFERENT
#     values, and the same check run against exoclimate #1 does fail. The error was
#     generalising from three samples of an intermittent effect, all of them among the
#     most RECENT merges, without ever comparing the pre-squash author.
#
# Counts above are a snapshot, and they INCLUDE the commit that writes them — the two
# previous versions of this comment each gave the parent's numbers, because they were
# read before committing. Recompute after the fact, not before:
#   git log --format='%an <%ae>' | sort | uniq -c
#
# Format: Proper Name <proper@email> <commit@email>

Jim Galasyn <jim.galasyn@hotmail.com> <jim.galasyn@confluent.io>
Jim Galasyn <jim.galasyn@hotmail.com> <jim.galasyn@hotmail.com>
