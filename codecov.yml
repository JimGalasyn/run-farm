# Coverage policy: 90% on new code, <0.5% project drift, without failing on
# sub-0.1% noise.
coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0.5%
    patch:
      default:
        target: 90%

comment: false
